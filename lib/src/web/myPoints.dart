


class MyPoints{
   final num lng;
   final num lat;
   final num count;

   MyPoints(this.lng, this.lat, this.count);

   @override
  String toString() {
    return 'MyPoints{lng: $lng, lat: $lat, count: $count}';
  }
}